model Category {
    id          String     @id
    name        String     @unique
    description String?
    isActive    Boolean    @default(true)
    medicines   Medicine[]
    createdAt   DateTime   @default(now())

    @@map("category")
}
